# Threefold — Router

**Read this first, then `STATE.md` for live truth, then `TRAPS.md` before touching code or infrastructure.**
Rules live here; state never does.

This repository is self-contained and governed by the project's root rules, STATE.md, and TRAPS.md.

## What this is

**Threefold**: Autonomous Coding Agent Governance, Cost Circuit-Breaker & Architectural Compliance Sidecar.
Target track: **Workplace Efficiency** (`#workplace-efficiency`).
Lane: **Community** (`#community`).
Done means: The application is deployed live on AWS, passes the Ship Gate, and every mandatory artifact is reachable by an anonymous visitor without an account.

## Hard Rules

1. **Deterministic gatekeeper:** Deterministic code trips circuit breakers and enforces boundaries; Amazon Bedrock provides semantic architectural explanations.
2. **Strict clean-room & privacy:** No real corporate names, proprietary repository IDs, or personal data. 100% synthetic agent sessions and workloads ("Acme DevCo").
3. **No local package installs:** Preserve the C: drive. No `pip install`, `npm install`, or heavy local builds. Use standard library and pre-existing pytest.
4. **Epistemic discipline:** Tag claims `[PRIMARY]` or `[STATE-FILE]`. Self-awarded scores are strictly labeled ESTIMATE.
5. **The four-file protocol:** This router, `STATE.md`, `LOG.md`, `TRAPS.md`. Never a fifth file of project state. Claim your area in `STATE.md` before touching code.
6. **Zero-setup visitor path:** Public demo executes the flagship claim (circuit breaker trip & governance certificate) in under 60 seconds with no account or credential.
