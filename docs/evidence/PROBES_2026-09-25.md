# Threefold live probes, 2026-09-25

- **Base:** https://raa131f9dj.execute-api.eu-west-1.amazonaws.com/prod/
- **Expected reads:** public (detected: public)
- **Run:** `probe-a71bf880` at 2026-09-25T10:08:12Z, 183 requests, 0 retried after 429
- **Key:** none
- **Writes:** `POST /simulate-loop`, which records a demo session under project `Acme-Sim`; `POST /simulate-secret`, which records a demo session under project `Acme-Sim`; synthetic calls under session ids `probe-a71bf880-*` and project `Acme-Probe`; calls under session ids `probe-a71bf880-*` sent as project `probe-a71bf880-not-acme`, recorded as `unlabelled`; sandbox `Acme-Sandbox-30032c61` with its seeded calls, reviews, promotion and demotion, which expires within 24 hours
- **Not deployed yet counts as:** SKIP
- **Result:** 113 PASS, 0 FAIL, 3 SKIP

Generated by `scripts/probe_live.py`. Each line is what the stack answered at the time of the run;
rerun the command to see it now.

## Latency

| Call | Samples | p50 ms | p95 ms |
|---|---|---|---|
| GET /status | 10 | 243 | 288 |
| POST /evaluate-tool-call | 20 | 295 | 317 |

## availability

| Check | Result | Evidence |
|---|---|---|
| page / | PASS | GET 200 text/html 64800 B, base path substituted; HEAD 200 empty |
| page /index.html | PASS | GET 200 text/html 64800 B, base path substituted; HEAD 200 empty |
| page /console.html | PASS | GET 200 text/html 3245 B, base path substituted; HEAD 200 empty |
| page /rules.html | PASS | GET 200 text/html 87746 B, base path substituted; HEAD 200 empty |
| page /connect.html | PASS | GET 200 text/html 71896 B, base path substituted; HEAD 200 empty |
| page /sessions.html | PASS | GET 200 text/html 32865 B, base path substituted; HEAD 200 empty |
| page /settings.html | PASS | GET 200 text/html 51214 B, base path substituted; HEAD 200 empty |
| page /swagger.html | PASS | GET 200 text/html 2811 B, base path substituted; HEAD 200 empty |
| page /dashboard.html | PASS | GET 200 text/html 169554 B, base path substituted; HEAD 200 empty |
| page /app | PASS | GET 200 text/html 169554 B, base path substituted; HEAD 200 empty |
| asset /assets/threefold.js | PASS | GET 200 text/javascript 50080 B, base path substituted |
| status | PASS | 200 application/json, status HEALTHY, 4 gates listed |
| readyz | PASS | 200 READY: SessionStore HEALTHY, BedrockExplanationModel HEALTHY |
| hook served as text | PASS | 200 text/plain, 119217 B of valid Python, sha256 024c50f8db54 |
| hook under its old names | PASS | /hooks/claude_code_hook.py and /claude_code_hook.py serve the same script |

## contract

| Check | Result | Evidence |
|---|---|---|
| openapi.json served | PASS | 200, 33 paths and 37 operations documented |
| POST /adapter/universal-tool-call | PASS | answered 400 (Bad Request) |
| POST /api/auth/links | PASS | answered 403 (Sign-In Is Closed Here) before routing: the guard proves the path is closed, not that the route behind it exists |
| POST /api/auth/sessions | PASS | answered 400 (Bad Request) |
| DELETE /api/auth/sessions | PASS | answered 200 |
| GET /api/auth/whoami | PASS | answered 200 |
| GET /api/decision | PASS | answered 400 (Bad Request) |
| GET /api/decisions | PASS | answered 200 |
| GET /api/insights | PASS | answered 200 |
| GET /api/overview | PASS | answered 200 |
| GET /api/projects | PASS | answered 200 |
| GET /api/projects/{name} | PASS | answered 200 |
| POST /api/projects/{name} | PASS | answered 403 (Projects Are Read Only Here) before routing: the guard proves the path is closed, not that the route behind it exists |
| POST /api/projects/{name}/demote | PASS | answered 403 (Projects Are Read Only Here) before routing: the guard proves the path is closed, not that the route behind it exists |
| POST /api/projects/{name}/promote | PASS | answered 403 (Projects Are Read Only Here) before routing: the guard proves the path is closed, not that the route behind it exists |
| POST /api/projects/{name}/reviews | PASS | answered 403 (Projects Are Read Only Here) before routing: the guard proves the path is closed, not that the route behind it exists |
| POST /api/sandbox | SKIP | creates a sandbox whatever its body says; the application group exercises it |
| GET /api/sessions | PASS | answered 200 |
| GET /dist/manifest.json | PASS | answered 200 |
| GET /dist/threefold-bundle.zip | PASS | answered 200 |
| POST /evaluate-tool-call | PASS | answered 400 (Bad Request) |
| GET /hooks/threefold_hook.py | PASS | answered 200 |
| GET /install.py | PASS | answered 200 |
| POST /issue-certificate | PASS | answered 400 (Bad Request) |
| GET /policy/config | PASS | answered 200 |
| POST /policy/config | PASS | answered 403 (Policy Is Read Only Here) before routing: the guard proves the path is closed, not that the route behind it exists |
| GET /proof.json | PASS | answered 200 |
| GET /readyz | PASS | answered 200 |
| GET /rules | PASS | answered 200 |
| POST /rules | PASS | answered 403 (Policy Is Read Only Here) before routing: the guard proves the path is closed, not that the route behind it exists |
| POST /rules/draft | PASS | answered 400 (Bad Request) |
| POST /rules/explain | PASS | answered 400 (Bad Request) |
| GET /sessions/{session_id} | PASS | answered 404 (No Such Session) |
| POST /sessions/{session_id}/resume | PASS | answered 403 (Sessions Cannot Be Resumed Here) before routing: the guard proves the path is closed, not that the route behind it exists |
| POST /sessions/{session_id}/terminate | PASS | answered 400 (Bad Request) |
| POST /simulate-loop | PASS | answered 200 |
| POST /simulate-secret | PASS | answered 200 |
| GET /status | PASS | answered 200 |
| application reads are documented | PASS | all 6 application reads that answer are documented |

## access

| Check | Result | Evidence |
|---|---|---|
| reads mode | PASS | anonymous GET /api/insights shows a public stack, as expected (public) |
| POST /policy/config needs the key | PASS | 403 Policy Is Read Only Here without a key |
| POST /policy/config refuses a wrong key | PASS | 403 Policy Is Read Only Here with a wrong key |
| POST /rules needs the key | PASS | 403 Policy Is Read Only Here without a key |
| POST /rules refuses a wrong key | PASS | 403 Policy Is Read Only Here with a wrong key |
| POST /sessions/<id>/resume needs the key | PASS | 403 Sessions Cannot Be Resumed Here without a key |
| POST /sessions/<id>/resume refuses a wrong key | PASS | 403 Sessions Cannot Be Resumed Here with a wrong key |
| POST /api/projects/Acme-X/promote needs the key | PASS | 403 Projects Are Read Only Here without a key |
| POST /api/projects/Acme-X/promote refuses a wrong key | PASS | 403 Projects Are Read Only Here with a wrong key |
| GET /api/insights public, names reduced | PASS | 200; every project is Acme-* or unlabelled and every developer an 8-hex hash |
| GET /api/sessions public, names reduced | PASS | 200; every project is Acme-* or unlabelled and every developer an 8-hex hash |
| GET /rules public, names reduced | PASS | 200; every project is Acme-* or unlabelled and every developer an 8-hex hash |
| GET /api/overview public, names reduced | PASS | 200; every project is Acme-* or unlabelled and every developer an 8-hex hash |
| GET /api/decisions public, names reduced | PASS | 200; every project is Acme-* or unlabelled and every developer an 8-hex hash |
| GET /api/projects public, names reduced | PASS | 200; every project is Acme-* or unlabelled and every developer an 8-hex hash |
| sandbox writes | PASS | Acme-Sandbox-a71bf880 is open to anyone (400 on a malformed body, not 401/403); 4 near misses are refused |

## governance

| Check | Result | Evidence |
|---|---|---|
| domain import via Write refused | PASS | a Write of `import boto3` into src/domain/ answered BLOCKED_BOUNDARY_VIOLATION |
| domain import via heredoc refused | PASS | a Bash heredoc of `import boto3` into src/domain/ answered BLOCKED_BOUNDARY_VIOLATION |
| domain import via redirect refused | PASS | a Bash redirect of `import boto3` into src/domain/ answered BLOCKED_BOUNDARY_VIOLATION |
| hook settings rewrite refused | PASS | a rewrite of .claude/settings.json answered BLOCKED_BOUNDARY_VIOLATION |
| git commit --no-verify refused | PASS | git commit --no-verify answered BLOCKED_BOUNDARY_VIOLATION |
| credential refused and not echoed | PASS | a command carrying an access key id answered BLOCKED_SECRET_DETECTED; the response does not echo it |
| page loop halts on the third call | PASS | APPROVED, APPROVED, BLOCKED_LOOP_DETECTED (halted); the next different call BLOCKED_CIRCUIT_BREAKER; GET /sessions/<id> is_tripped=true |
| hook loop refuses without halting | SKIP | Acme-Probe is in observe here: APPROVED, APPROVED, APPROVED, recorded and never halted; the refusal needs an enforcing project, which the application group checks after promotion |
| dry run observed, not refused | PASS | APPROVED with dry_run=True, observed python-domain-stays-pure, not halted |
| approval never reaches the model | PASS | APPROVED with explain=true, explanation_source=deterministic: no model call |
| refusal explained by the model | PASS | a page refusal carried explanation_source=bedrock |
| unknown project recorded as unlabelled | PASS | a project outside the pattern is answered with the 'unlabelled' warning |

## application

| Check | Result | Evidence |
|---|---|---|
| overview shape | PASS | window 7 days from rollups, 7 days in the series, 18 projects |
| overview totals equal its series | PASS | calls = approved + observed + refused: 559 = 354 + 29 + 176; would_refuse 29 against 29 observed in the series |
| decisions pagination | PASS | 4 pages, 12 rows, no duplicates; the second page is stable under its cursor |
| decision rows carry the contract fields | PASS | 12 rows carry rule_key, stage, hook_mode and review |
| decisions kind=refused filter | PASS | 50 rows, every one a refusal |
| decisions session filter | PASS | 1 row(s), every one of the session asked for, including the call this run recorded |
| decision drill-down | PASS | a listed row opens with decision, session and rule; an unknown verdict answers 404 |
| projects list shape | PASS | 18 projects, each with stage, readiness counts, agents and hook modes |
| project readiness | PASS | Acme-Probe: stage observe, 8 rules, every state follows from its counts |
| sandbox created | PASS | Acme-Sandbox-30032c61, 12 calls seeded, url dashboard.html#/projects/<name> |
| sandbox starts in observe with would-refuse rules | PASS | stage observe, sandbox true, would-refuse under 4 rules: python-domain-stays-pure, java-domain-stays-pure, web-domain-stays-pure, PROTECTED_PATH |
| hook call observed before promotion | PASS | a hook Write of `import boto3` into src/domain/: APPROVED with the would-refuse recorded, project_stage observe |
| reviews label its own rows and skip another project's | PASS | 3 rows under python-domain-stays-pure labelled correct; a row of Acme-Probe was skipped |
| review labels read back | PASS | 3 rows read back with review=correct and an 8-hex reviewed_by |
| reviewed rule becomes ready | PASS | python-domain-stays-pure is ready: 3 correct, 0 false alarms, 0 unreviewed |
| promote to enforce | PASS | stage enforce for python-domain-stays-pure, LOOP; 6 rules keep observing; history records it |
| hook call refused after promotion | PASS | the same hook call is BLOCKED_BOUNDARY_VIOLATION with project_stage enforce |
| rule left out keeps observing | PASS | a hook-settings rewrite is recorded, not refused, while PROTECTED_PATH keeps observing |
| hook loop refused without halting under enforce | PASS | APPROVED, APPROVED, BLOCKED_LOOP_DETECTED, APPROVED: the repeat refused, the session never halted |
| demote to observe | PASS | stage observe again; history records the demotion |
| hook call observed after demotion | PASS | after demotion the same hook call: APPROVED with the would-refuse recorded, project_stage observe |

## signin

| Check | Result | Evidence |
|---|---|---|
| sign-in flow | SKIP | no --key-file: minting a sign-in link needs the operator key |

## distribution

| Check | Result | Evidence |
|---|---|---|
| install.py names this stack | PASS | 101669 B of valid Python naming this base, no placeholder left |
| install.py matches its digest header | PASS | x-threefold-sha256 = sha256 of the body (573df8bf5e40...) |
| install.py matches the manifest | PASS | installer_sha256 equals the served install.py (573df8bf5e40...) |
| bundle matches the manifest file by file | PASS | bundle_sha256 matches; 15 files, each with the listed sha256 and size |
| bundled hook is the served hook | PASS | bin/threefold_hook.py in the bundle is byte for byte the served hook |

## headers

| Check | Result | Evidence |
|---|---|---|
| JSON routes say application/json | PASS | /status, /openapi.json, /rules, /api/insights answer application/json |
| errors are RFC 7807 problems | PASS | an unknown route (404) and a malformed body (400) are application/problem+json with type, title, status, detail |
| hook script headers | PASS | text/plain with X-Content-Type-Options: nosniff, so a browser shows it rather than runs it |
| CORS on a JSON read | PASS | Access-Control-Allow-Origin: * on a JSON read |
| CORS preflight for a keyed POST | PASS | the preflight allows POST with Content-Type, X-API-Key and Authorization from any origin |
| latency of GET /status | PASS | 10 calls: p50 243 ms, p95 288 ms |
| latency of 20 approved evaluate calls | PASS | 20 approved: p50 295 ms, p95 317 ms (budget 2000 ms) |
